Do the below programs in anonymous function & IIFE

(i)  Print odd numbers in an array
Ans:-
Let 'arr' be an array,
odd= arr.filter(number => {
    return number % 2 !==0;
});
console.log(odd);


(ii)  Convert all the strings to title caps in a string array
Ans:-
Let 'arr' be a string array,
upper= arr.map(element => {
    return element.toUpperCase();
});
console.log(upper);


(iii)  Sum of all numbers in an array
Ans:-
Let 'arr' be an array and sum= 0;
for(let i=0 ; i<arr.length ; i++){
    sum+= arr[i];
}
console.log(sum);


(iv)  Return all the prime numbers in an array
Ans:-
Let 'arr' be an array,
prime= arr.filter(number => {
    
})




Return all the palindromes in an array
Return median of two sorted arrays of the same size
Remove duplicates from an array
Rotate an array by k times
